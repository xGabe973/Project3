import * as React from 'react';
interface IOptions {
    actions?: IAction[];
    index?: number;
    level?: number;
    nodeOnly?: boolean;
    selector?: string;
    type?: string;
}
export interface IAction {
    condition: (node: Node, key: string, level: number) => boolean;
    post?: (node: Node, key: string, level: number) => React.ReactNode;
    pre?: (node: Node, key: string, level: number) => Node;
}
export declare function convertFromNode(input: Node, options?: IOptions): React.ReactNode;
export declare function convertFromString(input: string, options?: IOptions): React.ReactNode | Node;
export default function convert(input: Node | string, options?: IOptions): React.ReactNode | Node;
export {};
